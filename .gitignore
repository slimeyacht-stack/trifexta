# stray design playground (not part of the Trifexta site)
_designdemo.html

# OS / editor junk
.DS_Store
Thumbs.db
*.swp

# local preview server logs
*.log

# local config override — may contain a real (public) Snipcart key; keep it OUT of git
assets/config.local.js

# real audio (intentionally absent — drop your own mp3s locally, don't commit)
assets/audio/*.mp3
